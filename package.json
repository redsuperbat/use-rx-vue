{
  "name": "use-rx-vue",
  "version": "0.0.44",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist",
    "prebuild:package": "npm test && npm run clean && npm run compile && node dist/setupPackage.js",
    "build:package": "cd dist && npm publish",
    "test": "jest"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/redsuperbat/use-rx-vue.git"
  },
  "keywords": [],
  "author": "redsuperbat",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "@types/node": "^14.14.13",
    "jest": "^26.6.3",
    "rimraf": "^3.0.2",
    "rxjs": "^6.6.3",
    "ts-jest": "^26.5.0",
    "typescript": "^4.1.3",
    "vue": "^3.0.5"
  },
  "peerDependencies": {
    "rxjs": "^6.6.3",
    "vue": "^3.0.5"
  }
}
